# Email Configuration Example for Drinkmate
# Copy this file to .env and fill in your actual values

# Server Configuration
PORT=3000
JWT_SECRET=your_jwt_secret_here
FRONTEND_URL=http://localhost:3001
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/drinkmate

# Email Configuration (SMTP)
# Gmail Example:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Outlook/Hotmail Example:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_USER=your-email@outlook.com
# SMTP_PASS=your-password

# Custom SMTP Server Example:
# SMTP_HOST=mail.yourdomain.com
# SMTP_PORT=587
# SMTP_USER=noreply@yourdomain.com
# SMTP_PASS=your-smtp-password

# Alternative Email Variables (for backward compatibility)
EMAIL_USER=${SMTP_USER}
EMAIL_PASS=${SMTP_PASS}

# Cloudinary Configuration (for image uploads)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Security
CORS_ORIGIN=http://localhost:3001

# ===========================================
# Gmail Setup Instructions:
# ===========================================
# 1. Enable 2-Factor Authentication on your Gmail account
# 2. Generate an App Password:
#    - Go to Google Account settings
#    - Security > 2-Step Verification > App passwords
#    - Generate a new app password for "Mail"
#    - Use this password in SMTP_PASS
# 3. Make sure "Less secure app access" is OFF
# 4. Use your full Gmail address in SMTP_USER

# ===========================================
# Outlook Setup Instructions:
# ===========================================
# 1. Enable 2-Factor Authentication on your Outlook account
# 2. Generate an App Password:
#    - Go to Account settings > Security
#    - Advanced security options > App passwords
#    - Generate a new app password
#    - Use this password in SMTP_PASS
# 3. Use your full Outlook address in SMTP_USER

# ===========================================
# Custom SMTP Setup:
# ===========================================
# 1. Contact your hosting provider for SMTP details
# 2. Common ports: 587 (TLS), 465 (SSL), 25 (unencrypted)
# 3. Use your domain email address in SMTP_USER
# 4. Use your email password or SMTP password in SMTP_PASS
